version: 2.1

jobs:
  tests:
    docker:
      - image: cimg/python:3.9.7
    steps:
      - checkout
      - run:
          command: |
            . venv/bin/activate
            pip3 install -r requirements.txt
            pip3 install -r requirements.dev.txt
            pytest tests

workflows:
  ci:
    jobs:
      - tests
